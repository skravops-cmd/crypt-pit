apiVersion: v1
kind: Service
metadata:
  name: crypt-pit
  labels:
    app: crypt-pit
spec:
  selector:
    app: crypt-pit
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP