apiVersion: v1
kind: Service
metadata:
  name: {{ include "crypt-pit.fullname" . }}
  labels:
    {{- include "crypt-pit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crypt-pit.selectorLabels" . | nindent 4 }}