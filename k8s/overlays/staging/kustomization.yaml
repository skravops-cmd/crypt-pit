apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: crypt-pit-staging

resources:
  - ../../base

patchesStrategicMerge:
  - staging-patches.yaml

commonLabels:
  environment: staging

namespace: staging